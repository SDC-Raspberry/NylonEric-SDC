import http from 'k6/http';
import { sleep } from 'k6';

export default function () {
  // http.get('https://test.k6.io');
  // sleep(1);
  // server test:
  // products request:
  // product request:
  // styles request:
  // related request:
}